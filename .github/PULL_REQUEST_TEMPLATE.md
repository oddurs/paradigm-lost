## What this changes

<!-- One or two sentences. -->

## Checks

- [ ] `npm run check` passes (typecheck, build, layout + Forth verification)
- [ ] If this touches an exhibit: the listing is still period-accurate, and its
      badge still tells the truth about whether it executes or replays a trace
- [ ] If this adds a colour, size, or spacing: it comes from `design/`, not a literal
